#include <stdio.h>

/**
 * main - prtins number between 0 to  9 and lettrs between a to f.
 * return: Always 0 (pass)
 */
int main(void)
{
	int 1;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	for (i = 97; 1 < 103; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
