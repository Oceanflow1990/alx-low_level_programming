#include <stdio.h>

/**
 * main - prtins numbers frmo 0 to 9.
 * Return: always 0 (pass)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
