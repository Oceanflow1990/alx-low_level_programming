#include <stdio.h>

/**
 * main - prtins numbers frmo 0 to 9.
 * return: always 0 (pass)
 */
int main(void)
{
	int i;

	for (i = 0; 1 < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
